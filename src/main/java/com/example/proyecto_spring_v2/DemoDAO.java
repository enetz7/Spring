package com.example.proyecto_spring_v2;

import java.util.Optional;

import org.springframework.data.repository.CrudRepository;


public interface DemoDAO extends CrudRepository<Proyecto, Integer> {


	//save(S entity);

    //findOne(ID primaryKey);

    //findAll();

    //count();

    //delete(T entity);

	//exists(ID primaryKey);

}
